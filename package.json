{
  "name": "spile",
  "version": "0.1.0",
  "description": "A tiny local server that taps your claude.ai conversations into your own store",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "start": "node --experimental-strip-types src/server.ts",
    "dev":   "node --experimental-strip-types --watch src/server.ts",
    "lint":  "oxlint src",
    "fmt":   "oxfmt src"
  },
  "dependencies": {
    "hono": "^4",
    "@hono/node-server": "^1"
  },
  "devDependencies": {
    "@types/node": "^22"
  }
}
